# Qdrant_project
High-performance semantic search pipeline using Qdrant and Sentence-Transformers. Processes and embeds e-commerce data into vectors, enabling fast and accurate natural language search using cosine similarity instead of keyword matching.
